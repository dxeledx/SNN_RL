train:
  algo: ppo
  total_steps: 20000
  n_envs: 8
  rollout_steps: 128
  gamma: 0.99
  gae_lambda: 0.95

  lr: 3.0e-4
  update_epochs: 4
  minibatch_size: 256

  clip_coef: 0.2
  ent_coef: 0.01
  vf_coef: 0.5
  max_grad_norm: 1.0

  spike_rate_coef: 0.0

